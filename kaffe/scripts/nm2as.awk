#!/usr/bin/awk

# nm2as.awk
# Used only by kaffexprof 

BEGIN {
	printf("/* Automatically generated by nm2as.awk */\n");
}

/[0-9]/ {
	if( $1 != "U" && $1 != "w" )
	{
		printf(".weak %s\n", $3);
		printf("%s = 0x%s\n", $3, $1);
		if( $3 == "_etext" )
			exit 0;
	}
}

#eof
